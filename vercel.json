{
  "version": 2,
  "builds": [
    {
      "src": "src/api/**/*.py",
      "use": "@vercel/python",
      "config": {
        "installCommand": "echo \"No dependencies needed\""
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "src/api/$1.py"
    }
  ]
}
