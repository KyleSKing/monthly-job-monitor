# Job Monitor iOS App

## Development
1. Open JobMonitorApp.xcworkspace in Xcode.
2. Run pod install to install dependencies.
3. Build and Run on iOS Simulator.

## CI/CD
This project uses GitHub Actions for automated builds. Every push to eature/ios-app triggers the build and test pipeline.